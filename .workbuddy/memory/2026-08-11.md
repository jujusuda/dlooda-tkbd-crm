# 2026-08-11 工作记录

## 修复：今日视频不显示 + 寄样任务总计看不到（用户反馈"还是没有"）

### 根因1：日历跑到数据前面（视频/寄样/邀约今日全空）
- 实时时钟 8/11，但飞书数据只同步到 8/10（视频时间最新 8/8、sampleTime 最新 8/9、invite 最新 8/10）。
- 原 `getDailyReportData` 用 `getTodayStr()`（实时今天）做匹配 → 今日寄样/视频/邀约全部 0。
- **修复**：新增 `getDataToday()` —— 实时今天在数据里有活动就用实时今天；否则回退到 `max(最新sampleTime, 最新invite date)`（数据最新业务日）。日报"今日"锚定到它，视频再 T-2。
- 实测：锚定到 8/10 → 视频时间 8/8 → 19 个视频正常显示；今日寄样 21、邀约 4。
- 新增 `todayAnchored` 标记，界面标注「数据最新日 X，实时今天 Y 暂无数据」，避免用户以为统计错。
- **设计原则（重要）**：日报工具的"今日"应锚定数据最新日，而非实时时钟——否则任何时钟漂移都会让"今日"空白。

### 根因2：用户看不到已提交的改动（任务总计/视频修复）
- 上一轮已写好转码+总计代码并提交 97b0934，但用户仍看不到。
- 真凶：**sw.js 之前改成所有文件 stale-while-revalidate（缓存优先）** → 浏览器 reload 时先返回旧缓存 JS，新代码要第二次 reload 才生效，用户卡在旧版。
- **修复**：sw.js 改混合策略——`js/real-data.js`（2.8MB，只随同步变）仍缓存优先+后台刷新；其余 html/css/js 改**网络优先**，代码改动刷新即生效。CACHE_VERSION 升 v7。

### 验证手段（关键）
- 不能只信 data 层算对，要用 DOM 桩跑 `daily.js`/`task.js` 的 render，检查 `innerHTML` 真含「本月总计」「统计视频时间」「今日登记视频」等。本次实测全部 YES、视频数 19。

## 提交
- 7a8894a：视频锚定 + SW 混合缓存（4 文件）

## 第二轮修复（用户又反馈"视频没条数 + 计划条数改达人数 + 日期不能选以后"）

### 视频再加 fallback（双保险）
- 即使锚定到数据最新日，若 `today-2` 当天恰好无视频（数据缺口），原逻辑仍显示 0。
- 新增：若 `today-2` 匹配数为 0，自动回退到数据里**最新视频日期**再匹配一次。保证「今日视频」永远有最新一批，不会空白。实测 8/8 的 19 个视频正常显示。

### 邀请达人数口径（用户拍板）
- 飞书「邀约达人」表是**计划维度**（每行一个SKU邀约计划：日期/SKU/品类/佣金/计划/达成），`creatorId` 全部为空 → 无法按达人去重算真·达人数。
- 用户三选一选了「用触达人数(人)」：日报邀约卡片改为「今日邀约达人数 = 今日各邀约计划 achieved 累计（今日 18940 人）/ 4 个计划」。STAT_DEFS invite 项 label 改为「今日邀约达人数」，autoKey=todayInviteReach(人)，subKey=todayInviteCount(个计划)。

### 全模块日期限制"今天及以前"
- 用户要求所有时间选择器不能选今天以后。
- app.js createFilterBar：开始/结束日期 input 加 `max=getTodayStr()`，本月/近7天动作结束日不超过今天（已覆盖 dashboard/sample/report/invite/message/product/video/creator/ai-assistant 等所有筛选栏）。
- daily.js：日报日期 input `max=今天` + 后翻按钮若已在今天则禁止再后翻。
- todo.js：待办日期 input `max=今天` + 后翻按钮 forbid 越过今天。
- 注意：仅设 `max` 不够——后翻/前进按钮仍能跳到未来，必须同时 guard 按钮。

### 又发现：用户一直跑在旧缓存（关键坑）
- 用户截图仍显示 2026-08-09（pre-anchor 旧版），说明 v7 SW 没接管。
- 根因：虽然 sw.js 已有 skipWaiting + clients.claim、app.js 有 controllerchange→reload，但 **CACHE_VERSION 没升**，reg.update() 拿到的是同字节 sw.js，不触发新 SW 安装 → 旧 SW 继续服务旧缓存 JS。
- **修复**：CACHE_VERSION 升 v8，强制 SW 重新安装→接管→controllerchange→自动 reload，下一轮加载即拿到最新代码。
- **经验（重要）**：每次改了 JS/CSS 且要用户立刻看到，**必须 bump CACHE_VERSION**（即使 sw.js 逻辑没变，version 字符串变了 = 字节变了 = 触发更新）。

## 提交
- 2e45a1d：视频 fallback + 邀请达人数口径 + 全模块日期限制 + SW 升 v8

## 第三轮：用户用实际业务数据校验（8/10 应有18视频，8/11 应有6视频）

### 重新从飞书同步
- 运行 `python regen_realdata.py`，数据更新到 2026-08-11：
  - 每日寄样 2885 → 2881 条，邀约 438 → 417 条
  - 视频时间：8/8=19，8/9=7
  - 8/10 寄样 38（=8/9 的 21 + 8/10 的 17），视频 19（8/8）
  - 8/11 寄样 17（=8/10 的 17），视频 7（8/9），对应 6 位达人
- 用户说的「8/11 视频 6 个」其实是**达人数**（7 条视频里有一位达人发了 2 条，去重后 6 人）。
- 用户说的「8/10 视频 18 个」与系统 19 接近，可能是人工数漏或心中按达人去重后 18 位。

### 仍需确认
- 日报「今日登记视频」应显示「视频条数」还是「达人数」？当前显示条数（8/10=19，8/11=7）。若用户习惯按达人计，应改成「X 位达人（Y 条视频）」。

## 提交
- 5373dac：data: 重新从飞书同步 real-data.js（2026-08-11）

## 第四轮：今日登记视频改达人数口径（用户拍板）
- 用户确认：日报「今日登记视频」显示**达人数**为主、**视频条数**为副：「今日登记视频 6 位达人（7 条视频）」。
- daily.js STAT_DEFS video 项：autoKey 改 `todayVideoCreatorCount`(单位「位达人」)，subKey 改 `todayVideoCount`(单位「条视频」)。
- daily.js 预览章节标题改「今日登记视频（共 N 位达人 · M 条视频 · 视频时间 Z）」。
- sw.js CACHE_VERSION 升 v9。
- 实测：8/11 = 6 位达人（7 条视频）；8/10 = 19 位达人（19 条视频）。

## 提交
- dce7f84：今日登记视频改达人数口径 + SW 升 v9

## 第五轮：线上仍显示 0 视频（Cloudflare KV 旧映射）
- 用户截图显示 8/11 视频仍是 0，videoStatDate=8/9（说明 JS 代码已更新到达人数口径），但视频数为 0。
- 根因：项目同时有 Cloudflare Pages + KV 部署。`functions/[[path]].js` 让 `/js/real-data.js` 优先走 KV；而 `functions/lib/sync.mjs` 的视频时间字段映射还是旧的「视频N时间」，导致线上 KV 里的 real-data.js 视频时间全 null。
- 修复 `functions/lib/sync.mjs`：
  - 时间字段改为「视频时间」/「视频时间N」；
  - 链接字段支持「视频」/「视频N」；
  - 只要链接或时间任一存在就算一条视频（和本地 regen_realdata.py 对齐）。
- 修复 `sw.js`：统一网络优先（offline 回退缓存），避免 KV 更新后仍被浏览器旧缓存卡住。CACHE_VERSION 升 v10。
- 操作：推送后，用户需点右下角「📥 同步飞书」→「🔄 从飞书拉取」，触发 /api/sync 更新 KV，再刷新即可看到 8/11 = 6 位达人（7 条视频）。

## 提交
- c96f060：修复 Cloudflare 线上视频字段映射 + SW 网络优先

## 第六轮：本地视频正常，但 8/10 寄样量口径需调整 + 线上仍 0

### 用户校验本地数据
- 本地打开（用最新 real-data.js）视频能正常显示，说明 JS 代码 + 本地数据是对的。
- 用户反馈：8/10 日报寄样量应为 **21**（sampleTime = 2026-08-09），视频应为 **18**（videoTime = 2026-08-08）。
- 原代码 `todaySamples` 同时匹配 `today` 和 `prevDay`，导致 8/10 日报出现 38（8/9 的 21 + 8/10 的 17）。

### 修复寄样口径
- 飞书「寄样时间」是美国日期，比北京晚 1 天，所以日报日期 D 应对应 sampleTime = D-1（和视频 T-2 口径一致）。
- `getDailyReportData` 中 `todaySamples` 改为只匹配 `prevDayStr`。
- 8/10 日报：寄样 21（8/9），视频 19/18（8/8，取决于同步时间点）。

### 线上仍 0 的原因
- 用户已推送 c96f060（Cloudflare 14 分钟前已部署），但截图显示 Production 仍指向 7a8894a（15 小时前）。
- 即使 c96f060 生效，也需再触发一次「从飞书拉取」/api/sync 更新 KV，否则 KV 里还是旧映射数据（video.time 全 null）。
- 操作：等 Cloudflare 把 c96f060 设为 Production → 点同步 → 硬刷新。

## 第七轮：视频口径改为「更新时间=今天」（用户期望 5 达人 6 视频）

### 用户再次校验
- 用户截图：8/11 日报显示「今日登记视频 6 位达人（7 条视频）」，但应为 **5 位达人 6 条视频**。
- 飞书「每日寄样」表截图显示 5 行「更新时间 = 2026-08-11」且视频时间为 8/9 的记录；这正是用户心目中的「今天登记的视频」。
- 全库按 videoTime=8/9 统计会得到 6 达人 7 视频，多出来的是 `delaney.clapp`（updateTime 不是 8/11）。

### 修复视频统计口径
- 「登记」应以飞书行的 `updateTime` 为准，而不是只要有 videoTime=8/9 就算。
- `getDailyReportData` 中新增 `collectVideos(registrationDate, targetVideoDate)`：仅统计 `updateTime.startsWith(today)`（updateTime 为空则退到 sampleTime）且视频时间 = `today - 2` 的视频。
- fallback 仍保留：若当天无登记视频，回退到全库最新视频日期，避免「今天」空白。
- 实测：8/11 = 5 位达人（6 条视频），8/10 = 18 位达人（18 条视频），均与用户/飞书视图一致。

### 邀约达人数量线上 0 的问题
- 本地已正确显示 8/11 邀约 17435 人（3 个计划）。
- 线上 0 是因为 Cloudflare KV 里的 real-data.js 仍是旧数据（或 Pages Production 未指向最新 commit）。
- 本次一并推送最新 `js/real-data.js` + `js/data.js` + `js/daily.js` + `sw.js(v11)`。

### 提交并推送
- commit：data.js 视频改 updateTime 口径 + 寄样 prevDay + daily 提示 + SW v11 + real-data 最新
