# 2026-08-06 工作日志

## PWA 部署完善 — 局域网/手机访问
- 创建 `sw.js` Service Worker（预缓存应用外壳 + 离线缓存策略）
- 在 `app.js` 末尾注册 Service Worker（页面 load 后注册）
- 局域网访问地址：`http://192.168.1.5:8090`（WiFi IP，仅同网络可访问）
- 手机 PWA 安装：Android Chrome「安装应用」/ iOS Safari「添加到主屏幕」
- manifest.json 已存在（standalone 模式 + Hello Kitty 图标），无需修改
- 注意：http-server 关闭后需重启；如需公网访问需用 CloudStudio 部署
